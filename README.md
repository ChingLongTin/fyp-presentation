The presentation file is located in `presentation.typ`.

Note that we use nested highlighting from codly, we need to install `v1.3.1` of codly, which has not been distributed in preview yet.

To compile the file, you will need to follow https://github.com/typst/packages#local-packages and install `@local/codly:1.3.1`.